# GorjVPN-panel
GorjVPN یک راه‌حل مدرن و سبک برای دور زدن محدودیت‌های اینترنتی است که هیچ سرور مجازی (VPS) نیاز ندارد. کنترل اکانت‌ها، اعتبارسنجی دستگاه و تاریخ انقضا از طریق یک فایل رمزنگاری‌شده روی GitHub انجام می‌شود و ترافیک واقعی با استفاده از جعل SNI و Google Apps Script به عنوان رله عبور می‌کند. و همچنین پنل محور است.

# هسته
تمام هسته عبور از فیلترینگ متعلق به  https://github.com/therealaleph/MasterHttpRelayVPN-RUST که خود نسخه ای از https://github.com/masterking32/MasterHttpRelayVPN است

# این اسکریپت به صورت گرافیکی و ایمن می تواند یک script id را در چند کاربر پخش کند و ادمین بتواند دترسی هارا مشاهده یا محدود یا اضفه کند

# ساخت ID اختصاصی برای هر دستگاه
```
python GorjVPN_ID.py
```

# پنل کنترل کاربران 
```
python admincontrol.py
```

# اجرا روی کاربران
نکته: باید خروجی exe به افراد داده شود
وگرنه کلید قابل مشاهده است
```
python GorjVPN.py
```

# همچنین از لینک یک repositorie گیت هاب به عنوان سرور واسط بین ادمین و کاربران به صورت کاملا رمزنگاری شده استفاده می کند
```
# کلید رمزگذاری (بعداً می‌توانید آن را به یک مقدار قوی تغییر دهید)
ENCRYPTION_KEY = b'\x9f!\x8c\x7e\xd4\x1a\xb3\xc5\xe6\xf0\x2a\x3d\x5e\x7a\x8b\x9c\x0d\x1e\x2f\x3a\x4b\x5c\x6d\x7e\x8f\x9a\xab\xbc\xcd\xde\xef\xf0'
GITHUB_RAW_URL = "https://raw.githubusercontent.com/mr-r0ot/test/refs/heads/main/acc.txt" #لینک ارتباط بین کاربر و ادمین روی گیت هاب
```

در admincontrol.py و GorjVPN.py باید این دو خط با اطلاعات صحیح و یکسان جایگزین شوند
اولی کلید رمزنگاری و دومی لینک یه فایل روی گیت هاب به عنوان واسط است



# نکته بسیار مهم!
این پروژه تفریحی بوده (هرچند وقعا کار میکند) اما آسیب پذیر است زیرا با مهندسی معکوس ساده خروجی exe کلید رمزنگاری قابل مشاهده خواهد بود و در نتیجه script_id و auth_key لو میرود. مبهم سازی کد ضروری است اما باز هم تضمینی نیست!
